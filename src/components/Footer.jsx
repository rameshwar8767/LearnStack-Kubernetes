export default function Footer() {
  return (
    <footer className="footer">
      <div className="footer__inner">
        <span>LearnStack — a small catalog, built to be self-hosted.</span>
        <span className="footer__meta">Static build · shipped in a container · running on Kubernetes</span>
      </div>
    </footer>
  );
}
